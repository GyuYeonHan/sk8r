apiVersion: v1
kind: Secret
metadata:
  name: sk8tes-secrets
type: Opaque
stringData:
  # 32-byte base64 key (required)
  # Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
  appEncryptionKey: "REPLACE_WITH_BASE64_32_BYTE_KEY"
